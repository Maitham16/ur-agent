// QuerySource identifies what surface produced a query — used for routing
// and telemetry. Permissive string alias matches the wide set of literals
// observed across consumers.
export type QuerySource = string
